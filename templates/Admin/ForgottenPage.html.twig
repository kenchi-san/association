{% extends 'base.html.twig' %}

{% block body %}
    <div class="limiter">

        <div class="container-login100"
             style="background-image: url({{ asset('images_login/images_static-01.jpg') }});">
            <div class="wrap-login100 p-t-190 p-b-30">
                <div class="messageForUser">

                </div>
                <div class="container col-6">
                    <h3>Rentrez Votre adresse e-mail :</h3>
                    {{ form_start(form) }}
                    {{ form_row(form.mail) }}
                    <button class="btn btn-primary" type="submit"><span class="glyphicon glyphicon-ok-sign"></span> Envoyer</button>                    {{ form_end(form) }}
                </div>

            </div>
        </div>
    </div>


{% endblock %}
